using NoStdLib

import list

-- eachset : (a -> b) * Set(a) -> Set(a)
-- eachset(f, s) = set(list(s))

-- reducebag : (a × a → a) × a × Bag(a) → a
-- reducebag(f,z,b) = reduceset(f,z,each(fst,list(bagCounts(b))))

-- fst : (a × b) → a
-- fst (a,b) = a

reduceset : (a × a → a) × a × Set(a) → a
reduceset(f,z,s) = foldr(f,z,list(s))