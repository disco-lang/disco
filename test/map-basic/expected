setToMap : Set (ℕ × a) → Map ℕ a
setToMap({(1, 3), (4, 6)}) : Map ℕ ℕ
setToMap({(1, 3), (4, 6)})
insert : ℕ × a × Map ℕ a → Map ℕ a
emptyMap : Map ℕ a
emptyMap
insert(1, 3, insert(4, 6, emptyMap)) : Map ℕ ℕ
setToMap({(1, 3), (4, 6)})
setToMap({(1, 3)})
lookup : ℕ × Map ℕ a → Unit + a
right(3)
right(6)
left(())
